# Fase 0 — mínimos que se probaron. Fijar versiones exactas al final de la Fase 2 (pip freeze).
pandas>=2.2
numpy>=1.26
openpyxl>=3.1
matplotlib>=3.8
openai>=1.30
pytest>=8
# Capa de argumentos (Fase 1): scikit-learn, umap-learn, hdbscan. python-louvain ya no compila
# con setuptools recientes; networkx trae louvain_communities si hace falta.
